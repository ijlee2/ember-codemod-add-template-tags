type CodemodOptions = {
  componentStructure: 'flat' | 'nested';
  projectRoot: string;
};

type Options = {
  componentStructure: 'flat' | 'nested';
  projectRoot: string;
};

export type { CodemodOptions, Options };
